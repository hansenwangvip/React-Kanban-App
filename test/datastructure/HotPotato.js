/** * Created by kw on 17/7/5. */function Queue() {	var items = [];	this.enqueue = function (ele) {		items.push(ele);	};	this.dequeue = function () {		return items.shift();	}	this.front = function () {		return items[0];	}	this.isEmpty = function () {		return items.length === 0;	}	this.size = function () {		return items.length;	}	this.print = function () {		console.log(items.toString());	}}function hotPotato(nameList, num) {	var queue = new Queue();	for (var i = 0; i < nameList.length; i++) {		queue.enqueue(nameList[i]);	}	console.log("一开始有" + queue.size() + "个人！")	var eliminated = '';	while (queue.size() > 1) {		for (var i = 0; i < num; i++) {			queue.enqueue(queue.dequeue())		}		eliminated = queue.dequeue();		console.log("现在 " + eliminated + " 被淘汰了！");		console.log("现在还有 " + queue.size() + " 个人！")	}	return queue.dequeue();}var names = ['John', "Jack", 'Camila', 'Indgrid', 'Carl'];var winner = hotPotato(names, 7);console.log(winner + "胜出！");